import React, { Component } from "react";
import "./index.css";
import { Layout, Row, Col } from "antd";
const { Header } = Layout;

export default class StemHeader extends Component {
  render() {
    return <Header></Header>;
  }
}
